public interface Shape {
    
    Double perimetro();
    Double area();
    
    /**
     * 1)crea un modo per identificare la forma da calcolare
     * 2)creare  i metodi di calcolo dell'area e del perimetro
     */
}
